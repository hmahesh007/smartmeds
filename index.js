import React from 'react';
import ReactDOM from 'react-dom';
import App from '/Users/arnavdixit/Downloads/hfh_copy/src/App.tsx'; // Import your main component (replace 'App' with your actual main component)

ReactDOM.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
  document.getElementById('root')
);